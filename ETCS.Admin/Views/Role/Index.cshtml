@using ETCS.Admin.Infrastructure.Auth
@{
    ViewData["Title"] = "Manage Roles";
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.AdminModuleKey = "Role";
}
<div class="card">
    <div class="card-body">
        <div class="admin-grid-toolbar">
            <partial name="_AdminGridSearch" />
            @* <div class="admin-grid-actions">
                @if (AdminPermissionViewHelper.CanAdd(User, "Role"))
                {
                    <button type="button" class="btn btn-outline-primary" onclick="loadRole(0)"><i class="ti ti-plus"></i> Add Role</button>
                }
            </div> *@
        </div>
        <div class="table-responsive admin-grid-table-scroll">
        <table id="grid_table" class="table table-striped table-bordered w-100" style="width:100%">
            <thead>
                <tr>
                    <th>Role</th>
                    <th>Full Access</th>
                    <th>System</th>
                    <th>Active</th>
                    <th>Users</th>
                    <th class="text-center" style="width:70px"></th>
                </tr>
            </thead>
        </table>
        </div>
    </div>
</div>
<div class="modal fade" id="addDataModal" tabindex="-1" data-bs-backdrop="static">
    <div class="modal-dialog modal-dialog-centered modal-xl">
        <div class="modal-content" id="div_add"></div>
    </div>
</div>
@section Scripts { <script src="~/js/custom/role.js"></script> }
