@using ETCS.Shared.Infrastructure.Admin.Inventory.MealEnums
@using ETCS.Shared.Infrastructure.Admin.Reports.MealOrders
@{
    ViewData["Title"] = "Meal Order Report";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var schools = ViewBag.Schools as IReadOnlyList<MealOrderSchoolLookupDto> ?? Array.Empty<MealOrderSchoolLookupDto>();
    var mealSessions = ViewBag.MealSessions as IReadOnlyList<MealEnumLookupDto> ?? Array.Empty<MealEnumLookupDto>();
    var startDate = DateTime.Today.AddDays(-1).ToString("yyyy-MM-dd");
    var endDate = DateTime.Today.ToString("yyyy-MM-dd");
    var reportError = TempData["ReportError"] as string;
}

@if (!string.IsNullOrWhiteSpace(reportError))
{
    <div class="alert alert-warning">@reportError</div>
}

<div class="card mb-3">
    <div class="card-body">
        <div class="row g-3">
            <div class="col-12 col-md-3 col-lg-4">
                <label for="txtStartDate" class="form-label">Start date</label>
                <input type="date" id="txtStartDate" class="form-control" value="@startDate" />
            </div>
            <div class="col-12 col-md-3 col-lg-4">
                <label for="txtEndDate" class="form-label">End date</label>
                <input type="date" id="txtEndDate" class="form-control" value="@endDate" />
            </div>
            <partial name="_ScopedMealSchoolFilter" />
            <div class="col-12 col-md-6 col-lg-4">
                <label for="ddlMealSession" class="form-label">Meal session</label>
                <select id="ddlMealSession" class="form-select">
                    <option value="">All Sessions</option>
                    @foreach (var session in mealSessions)
                    {
                        <option value="@session.Id">@session.Name</option>
                    }
                </select>
            </div>
            <div class="col-12 col-md-6 col-lg-4">
                <label for="ddlMealType" class="form-label">Meal type</label>
                <select id="ddlMealType" class="form-select" disabled>
                    <option value="">All Meal Types</option>
                </select>
            </div>
            <div class="col-12">
                <button type="button" class="btn btn-primary" id="btnViewReport">
                    <span class="btn-label">View Report</span>
                    <span class="btn-spinner spinner-border spinner-border-sm ms-1 d-none" role="status" aria-hidden="true"></span>
                </button>
                <button type="button" class="btn btn-outline-primary" id="btnExportReport">Export Report</button>
            </div>
        </div>
    </div>
</div>

<form id="frmExport" asp-controller="Report" asp-action="ExportMealOrdersMealDb" method="post" class="d-none">
    @Html.AntiForgeryToken()
    <input type="hidden" name="StartDate" id="exportStartDate" />
    <input type="hidden" name="EndDate" id="exportEndDate" />
    <input type="hidden" name="SchoolId" id="exportSchoolId" />
    <input type="hidden" name="MealSessionId" id="exportMealSessionId" />
    <input type="hidden" name="MealTypeId" id="exportMealTypeId" />
</form>

<div id="mealOrderReportLoader" class="canteen-report-screen-loader d-none" aria-live="polite" aria-busy="true">
    <div class="canteen-report-loader-card">
        <div class="spinner-border text-primary" role="status">
            <span class="visually-hidden">Loading...</span>
        </div>
        <span class="canteen-report-loader-text">Loading report...</span>
    </div>
</div>

<div class="card canteen-report-card">
    <div class="card-body">
        <div id="reportEmptyMessage" class="text-muted mb-2">Click View Report to load data.</div>
        <div id="mealOrderReportGridWrap" class="canteen-report-grid-wrap">
            <div class="table-responsive canteen-report-table-scroll">
                <table id="grid_table" class="table table-striped table-bordered canteen-report-table">
                    <thead>
                        <tr>
                            <th>S No</th>
                            <th>Meal Date</th>
                            <th>Student Card No.</th>
                            <th>Student Name</th>
                            <th>Grade</th>
                            <th>Section</th>                            
                            <th>Payment Status</th>
                            <th>Meal Session</th>
                            <th>Meal Type</th>
                            <th>Choice</th>                            
                            <th>Day</th>
                            <th>Items</th>
                            <th>Order Date</th>
                        </tr>
                    </thead>
                </table>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/js/custom/meal-order-mealdb-report.js"></script>
}
